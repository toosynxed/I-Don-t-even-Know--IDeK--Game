{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug IDeK terminal game",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main_terminal.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}